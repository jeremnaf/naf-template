# ${{ values.name }} capability

This repository has been generated by [Naftiko Warden](https://shipyard.naftiko.io/docs/1.0.0-alpha3/fleet/warden/), a custom backstage template.

## Content

### The capability file(s)

The `${{ values.name }}.ikanos.yml` file is your main capability file. This is the file which is used by the Ikanos server to publish your capability. It may not be complete and you may need to complete it manually. For this, you can use [Naftiko Crafter](https://shipyard.naftiko.io/docs/1.0.0-alpha3/fleet/crafter/), a VS Code plugin.

Depending on wheter you're using Apis from your backstage catalog or not, you can also find other capability files `*-consumes.yml`.

### Catalog info

The `catalog-info.yaml` file is useful for the backstage application. It allows you to add a new service component corresponding to this capability in the catalog.
